// Mockup preview route — http://localhost:3000/mockups/fire-panel
// Renders the standalone FIRE panel mockup so you can iterate on visuals
// without touching the working scenario code.

import EngineFirePanel from "@/components/cockpit/engine-fire-panel-mockup";

export default function FirePanelMockupPage() {
  return <EngineFirePanel />;
}
